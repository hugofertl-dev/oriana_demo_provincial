# Local Netlify folder
.netlify

# Secrets locales (API keys de ElevenLabs, etc.) — NUNCA commitear
.env
.env.*

# Dependencies (netlify-cli, jsdom — installed with --no-save)
node_modules/

# OS / editor cruft
.DS_Store
*.log

# Python bytecode (de py_compile en verify)
__pycache__/
*.pyc
