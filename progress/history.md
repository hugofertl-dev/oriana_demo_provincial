# Historial de sesiones

> Una línea por sesión o ajuste directo: `AAAA-MM-DD — qué se hizo (ids si aplica)`.
> Los detalles viven en progress/archive/<id>/; esto es solo la cronología.

2026-07-06 — Feature reclamos-tickets: gestión de reclamos (luz / hospitales públicos) con tickets + pantalla "Mis reclamos". Cerrada tras review (fix bloqueante de ruteo vs línea 144). 34/34 tests. Luego: paso de dirección del reclamo eléctrico a carga libre (localidad + calle/número, Misiones).
2026-07-06 — Feature chat-llm-misiones: chat de ORIANA con Claude vía /api/chat (datos de Misiones, fallback a regex). Cerrada tras reviewer + security-auditor (contexto fresco): XSS descartado; fixes de idempotencia, action incompleta, error genérico y CORS configurable. Deuda aceptada: endpoint público sin rate-limit. 44/44 tests.
2026-07-07 — Feature ui-healthcare-redesign: rediseño de UI completo al lenguaje "Healthcare Chatbot" (paleta verde, tokens en :root, bot sin burbuja, chips pill, header pill, tabbar). CSS-first (lógica intacta). Cerrada tras reviewer APROBADO. Logo estrella con degradé verde (sin gota). LLM revalidado en vivo post-rediseño. 44/44 tests.
2026-07-06 — Ajuste: naturalidad de voz TTS → EL_STABILITY 0.6→0.4 y EL_STYLE 0.15→0.10 (index.html + oriana-mobile.html). Verificado con /api/tts (200, audio/mpeg). También: .env agregado a .gitignore.
2026-07-06 — Ajuste: se eliminó el soporte de Vercel; queda solo Netlify. Borrado api/tts.js; limpiadas menciones a Vercel en COMO-PUBLICAR.md, CLAUDE.md, docs/architecture.md y docs/lessons.md. verify quick verde.
2026-07-06 — Ajuste: voz TTS variante #4 → EL_STABILITY 0.35, EL_STYLE 0.05 (index.html + oriana-mobile.html) y similarity_boost 0.50→0.75 (netlify/functions/tts.js). Elegida por el usuario entre 5 candidatas A/B.
2026-07-06 — Feature permiso-microfono: panel de permiso de micrófono (iOS/Safari) con engranaje + apertura al tocar 🎙️. Cerrada tras reviewer + aceptación en iPhone real. En el camino se arregló el autoplay de voz en iOS y se documentaron 2 gotchas iOS en lessons. Deploy en Netlify (pre_produccion, auto-deploy desde GitHub).
2026-07-06 — Feature login-cuentas: login + crear cuenta + persistencia en localStorage, cerrar sesión en Perfil, datos de la cuenta en Perfil/Inicio. Cerrada tras reviewer (2 fixes menores aplicados: escape XSS + sesión huérfana). Incluyó fix de bug pre-existente (.screen.hidden vs display por ID).
2026-07-07 — Feature geo-cercania: geolocalización para hospital/comisaría más cercano (modal de permiso estilo mic, distancia haversine real desde coords de Posadas, anclaje >100 km, fallback por dirección). Integrada al LLM (action ubicacion_cercana), inferencia de contexto (recordLLM), y manejo de error de conexión (aviso + reintentar, degradación tras 3 fallos). +Ajuste texto en Inicio. Cerrada tras reviewer APROBADO (fix: dismiss del modal suelta flow; -código muerto). 63 tests.
2026-07-07 — Chore: se agregaron 6 imágenes de eventos a assets/ (eventos) para la futura refactorización de la pantalla Eventos (todavía sin referenciar en el código). Nota: alta resolución (~12 MB total) — optimizar/redimensionar y renombrar a url-safe al usarlas.
2026-07-07 — Feature tabbar-siempre-visible: tabbar (Actividad/Inicio/Eventos) siempre visible y funcional en toda pantalla post-login (oculta solo en login/signup); composer del chat apilado encima; "Eventos" activa en detalle de evento, ninguna activa fuera de las 3 secciones. Cerrada tras reviewer APROBADO (1 menor aplicada) + aprobación visual del usuario. 63+27 tests.
2026-07-07 — Feature eventos-fotos-reales: las 6 fotos reales reemplazan los gradientes placeholder en mosaico y detalle de Eventos (assets optimizados con sips a ~1000px/≤150 KB, url-safe; originales de 12 MB eliminados; emoji decorativo fuera). Cerrada tras reviewer APROBADO (2 menores resueltas) + aprobación visual del usuario. 63+27+40 tests.
2026-07-07 — Ajuste: header de Eventos clonado del de Actividad (.hdr.plain "Eventos"; fuera ☰/▦/🔔/avatar y brand), filtro "Mapa" eliminado (botón + branch de renderLinda), y botón "Cómo llegar" quitado del detalle de evento (queda Guardar). CSS muerto removido. verify full verde.
2026-07-07 — Fix voz-horas: horaPalabras pasa a formato hablado de 12 h con momento del día ("15:40" → "tres y cuarenta de la tarde", "una" en femenino, mediodía), el "·" de la agenda se dice "a las", y las abreviaturas con punto (Dr./Av./Tel.) ya no dejan el punto colgado (\b tras punto → lookahead). Validación end-to-end: el flujo regex no lee opciones por voz; lo que se escucha es el reply del LLM vía speakable. +10 checks en test/speakable.test.js (rojos antes) + sabotage OK. 160 tests verdes.
2026-07-07 — Auditoría profunda de estabilidad móvil (3 auditores paralelos: compat iOS/Android, robustez JS/races, backend/performance): 28 hallazgos nuevos verificados en código. Resultado en PLAN_ESTABILIDAD.md (raíz, 5 fases priorizadas) + entrada planned estabilidad-movil en feature_list.json. Nada implementado aún.
2026-07-07 — Fix voz-abreviaturas: speakable() ahora dice los días abreviados de la agenda con nombre completo ("Mié 8/07" → "miércoles ocho de julio"; "Mar" solo antes de número para no pisar la palabra "mar") y se arregló la regla "24 h" que nunca funcionó (corría después de convertir enteros). Test test/speakable.test.js (20 checks, rojo antes del fix) + sabotage check OK + verify full verde.
2026-07-07 — Fix: mic sordo en iOS tras respuesta TTS (patrón audio→texto→audio). Diagnóstico con overlay ?micdebug en iPhone (4 iteraciones): iOS retiene la sesión del <audio> ~15-60 s y el dictado nace sin captura. Cura: TTS por AudioContext con suspend() al terminar (webAudioPlay); <audio> primeado de fallback + refresh de captura/espera de unmute como defensa. Verificación manual acordada en iPhone del usuario: conversación completa por voz OK. Lección en docs/lessons.md; debug removido.
2026-07-07 — estabilidad-movil FASE 1 (críticos, PLAN_ESTABILIDAD.md ítems 1-6): timeouts de red en todos los caminos (fetchTimeout 15 s /api/chat y 10 s TTS en cliente; Anthropic timeout:8000/maxRetries:0 en chat.js; AbortSignal.timeout(8000) en tts.js), 100dvh + safe-area-inset en .phone/#tabbar/#screen-home/.composer, meta viewport con viewport-fit=cover + interactive-widget=resizes-content y handler visualViewport (--kb) para el teclado iOS, reset de conversación con epoch (llmEpoch) que descarta respuestas LLM en vuelo + vacía llmQueue, tope de llmHistory (≤16, primero user) y "quiero" fuera de RX_YES. Test nuevo test/estabilidad-fase1.test.js (9 checks rojos antes del fix) + sabotage OK + verify full verde + screenshots desktop píxel-idénticas a HEAD. PENDIENTE: verificación manual en celular iOS/Android de viewport/teclado (ítems 2-3).
2026-07-07 — Fix voz-fecha-teléfonos: la regla de teléfonos de speakable() (dígito a dígito) corría ANTES que las de hora/fecha y se comía "07 09" de "Mié 8/07 09:30" (formato sin "·" que usa el LLM al listar horarios) → la voz decía "cero siete cero nueve". Reordenada después de horas/fechas + "a las" también cuando fecha y hora van separadas por espacio. Detectado en celular por el usuario tras el deploy de FASE 1; repro con LLM real (la batería inicial chequeaba dígitos sobrevivientes y este bug producía PALABRAS de dígitos — moraleja para futuros checks de voz). +4 checks en speakable.test.js (2 rojos antes) + sabotage OK + verify fix verde.
2026-07-07 — Ajuste: timeout del SDK Anthropic en chat.js 8000→9000 ms (el usuario veía 502 "Request timed out" seguido; 9 s deja ~1 s de margen contra el corte de ~10 s de Netlify). verify quick verde.
2026-07-07 — estabilidad-movil FASE 2 (voz confiable, ítems 7-11): errores del dictado visibles en el placeholder (network/no-speech/audio-capture ya no se tragan), dictado en es-AR con fallback es-419→es-ES (reintento en onend si language-not-supported), un borrador tipeado ya no se auto-envía al cancelar el dictado (flag gotResult), el mic se libera al terminar cada dictado (releaseMic; permiso pasa a flag _micGranted para no depender del stream vivo) y el AudioContext "interrupted" se descarta al volver a foreground + resume() con carrera de 1.2 s en webAudioPlay. Ítem 12 (auto-envío en primera pausa) pendiente de decisión de producto. Test test/estabilidad-fase2.test.js (9 checks rojos antes) + sabotage OK + verify fix verde. PENDIENTE verificación manual en celular: dictado con voseo, indicador de mic apagado, voz tras bloquear pantalla.
2026-07-07 — estabilidad-movil FASE 3 (robustez del chat, ítems 13-18): goChat→smartHandle (respeta el lock del LLM), el flow loc expira al salir del chat (show limpia y cierra el modal), openGeoPanel vacía llmQueue (mensaje encolado ya no se come como dirección), dobles disparos resueltos con flow sincrónico (start* setean flow antes del typing; handle reestructurado) + debounce global de chips (900 ms, listener en captura), getCurrentPosition cancelable por secuencia (_geoSeq; cerrar modal/navegar descarta la respuesta) y "cancelar" tipeado cierra el modal, y back físico de Android navega entre pantallas (pushState por pantalla + popstate→show). Test test/estabilidad-fase3.test.js (11 checks rojos antes) + sabotage OK + verify fix verde (suite completa: los flujos regex existentes intactos). PENDIENTE manual: back de Android en dispositivo.
2026-07-07 — estabilidad-movil FASE 4 (performance, ítems 19-22 y 24): TTS a eleven_flash_v2_5 (tts.js y camino directo; VALIDAR con el oído que la voz argentina suene bien — si no, volver a multilingual_v2), warm-up OPTIONS a /api/chat y /api/tts al cargar (cold start fuera del 1er mensaje, sin tokens), [[headers]] en netlify.toml con Cache-Control immutable para /assets/*, fotos recomprimidas a 640px calibrando calidad por archivo (756 KB→357 KB, receta sips de lessons), y 429/529 con mensaje "dame unos segundos" + chip Reintentar deshabilitado 5 s sin contar para llmDisabled. Ítem 23 (cache CDN del TTS fijo) DESCARTADO con razón: /api/tts es POST y el CDN no cachea POST; la alternativa (pre-generar audios como assets) es infra aparte — si el saludo pesa en la factura, retomarlo. Test test/estabilidad-fase4.test.js (5 checks rojos antes) + sabotage OK + verify fix verde.
2026-07-07 — estabilidad-movil FASE 5 (menores/deuda, ítems 25-31): audioCache FIFO de 30 con revokeObjectURL, persistencia de demo en localStorage (turnos/reclamos/favoritos sobreviven al refresh; decisión del usuario) con reset en "Cerrar sesión", sin autofocus del input en táctil (pointer:coarse), window.scrollTo eliminado de bubble() (saltos iOS), landscape a pantalla completa vía (max-height:500px), publish desde dist/ (scripts/stage_publish.sh — docs/, progress/, test/ y CLAUDE.md ya no se publican por URL; OJO: verificar que el primer deploy de Netlify con build command salga verde), y tts.js ya no reenvía el detail crudo de ElevenLabs (queda en el log del server). Ítem 12: queda el auto-envío del dictado (decisión del usuario). Ítem 32 (typing solapados, cosmético): no encarado. Test test/estabilidad-fase5.test.js (10 checks rojos antes) + sabotage OK + verify full verde. PLAN_ESTABILIDAD.md marcado IMPLEMENTADO 5/5.
2026-07-07 — estabilidad-movil: review final (subagente reviewer, contexto fresco) = APROBADO CON OBSERVACIONES; las 3 aplicadas: (1) logout ahora recarga la página (la DB en memoria re-persistía los datos viejos tras re-login), (2) payload persistido versionado {v:1} (shape viejo se descarta al boot), (3) debounce de chips pasa de global a POR ELEMENTO (tocar rápido un chip distinto vuelve a ser válido). Tests actualizados (fase3 +1 check, fase5 seed versionada) + verify full verde. Verificación en dispositivo del usuario: deploy dist/ verde, voz flash bien, dictado/mic/back OK.
2026-07-07 — estabilidad-movil CERRADA (feature-close): archivada en progress/archive/estabilidad-movil/ (summary + PLAN_ESTABILIDAD.md movido ahí), entrada eliminada de feature_list.json, lección del orden de reglas de speakable() en docs/lessons.md. Gate de seguridad: security_scan full sin bloqueantes (2 CVEs menores en deps dev: @opentelemetry/core y esbuild, no llegan a producción) + auditor en contexto fresco: 2 hallazgos BAJOS confirmados y APLICADOS (esc() en Ticket ${r.id} de renderReclamos — XSS con precondición local vía localStorage manipulado; catch de tts.js ya no devuelve e.message crudo). A verificar a futuro: context del LLM incluye datos persistidos (hoy inocuo: actions validadas por schema); netlify.toml sin CSP/X-Frame-Options (pre-existente). OJO harness: gitleaks del security_scan corrió sobre staging VACÍO (0 bytes) — con todo commiteado no escanea nada; revisar el script.
2026-07-07 — FIX dictado doble envío: si el usuario tocaba Send mientras el dictado seguía activo, el onresult FINAL de SpeechRecognition (llega DESPUÉS del tap) rellenaba el input y onend lo auto-enviaba otra vez → mensaje duplicado (y además pisaba replyWithVoice=true). Fix: setupMic expone cancelPending() (flag squelched + rec.abort()); sendMain/sendNinos la llaman primero — el envío manual gana y descarta lo pendiente. El auto-envío del dictado normal (ítem 12, decisión del usuario) se mantiene intacto. Test test/dictado-doble-envio.test.js (6 checks rojos antes) + sabotage OK + verify fix verde. Verificado en el celular del usuario (deploy eb5538c): entra una sola vez. CERRADO.
2026-07-07 — PLAN_PRODUCCION.md creado en la raíz: roadmap en 5 fases para pasar de demo estable a app probada desde todos lados (blindaje de APIs/rate-limit, telemetría de errores, matriz multi-navegador/WebView, oriana-demo.html podrido, endurecimiento y deuda del harness). 18 puntos, con decisiones ⚠️ y acciones manuales 🖐️ marcadas. Origen: relevamiento post-cierre de estabilidad-movil.
2026-07-07 — Ítem 7 (FASE 2, smoke test post-deploy): scripts/smoke.sh <url> [--light]. Completo (default): home 200+"ORIANA" + POST real a /api/chat (verifica {reply} → caza LLM_API_KEY faltante) + POST real a /api/tts (verifica audio/mpeg → caza ELEVENLABS_* faltantes); cuesta ~centavos/corrida. --light: sin llamadas pagas (home + OPTIONS de routing). Decisión del usuario: real por defecto + flag light. Validado contra el deploy real (ambos modos verdes). Nota agregada en COMO-PUBLICAR.md. FASE 2 completa (ítems 5-7). Tratado como tooling/Ajuste (no toca la app). No va a verify.sh porque necesita una URL viva.
2026-07-07 — Fix reintento-chat: el chat LLM ahora reintenta UNA vez en silencio (typing visible, respiro de 600 ms) ante un bache transitorio —timeout/red o 5xx del server— antes de mostrar "problema de conexión 📡"; 429/529 (saturado) y 4xx NO se reintentan (van directo a su aviso). Antes: 0 reintentos, el aviso salía al primer fallo. +timeouts subidos un poco: fetch cliente /api/chat 15s→18s y SDK Anthropic en chat.js 9s→9,5s (techo de ~10s de Netlify; más aire requiere subir plan). llmHandle reestructurado con intento() interno reintentable. Detectado un race en el test (el warm-up OPTIONS robaba respuestas de la cola → el mock ahora lo ignora). Test test/reintento-chat.test.js (10 checks, rojo-primero + sabotage OK) + verify full verde (suite completa intacta, fase4 incluido). Verificado en el celular del usuario tras deploy (3f470b2): el mensaje de conexión bajó notablemente. CERRADO.
2026-07-07 — Feature telemetria-log (FASE 2 del PLAN_PRODUCCION, ítem 5 + parte del 6): captura global de errores del cliente (window error/unhandledrejection en index.html + espejo oriana-mobile.html) → POST a /api/log propio (NO Sentry, decisión del usuario) con {msg,stack≤1000,ua,screen,url,ts} SIN datos del ciudadano; anti-loop tope 10/sesión + el POST se traga cualquier fallo. netlify/functions/log.js: POST persiste en Upstash (LPUSH errlog + LTRIM 0 199 + EXPIRE 7d) + console.error (panel Netlify), 204 fail-open; GET lee últimos 50 con token por header Authorization (NO query string) + rate-limit propio (logread 10/min) + nosniff. Reusa _ratelimit.js. Reviewer APROBADO (se quitó flag _errSending inútil) + security-auditor: 2 MEDIOS corregidos (token a header + GET rate-limited), 1 bajo asumido por diseño (log-drowning), sin inyección Redis; 2 "a verificar" a futuro (escapar en un panel lector; PII en errores con datos reales). Test test/telemetria-log.test.js (19 checks, rojo-primero + sabotaje de cap y token) + verify full verde + security_scan sin bloqueantes. PENDIENTE: 🖐️ cargar LOG_READ_TOKEN (aleatorio ≥32 chars) en Netlify; verificación e2e contra deploy (forzar un error → que llegue al log). Ítem 7 (smoke test) pendiente.
2026-07-07 — Feature rate-limit-apis (FASE 1 del PLAN_PRODUCCION, ítems 1-2): rate-limit por IP con Upstash Redis (REST, INCR+EXPIRE NX atómico) en netlify/functions/_ratelimit.js (helper _-prefijado, no es endpoint); chat.js 20 req/min y tts.js 40 req/min → 429 con Retry-After antes de pegarle al LLM/ElevenLabs (el cliente ya maneja 429). FAIL-OPEN (decisión del usuario): sin env vars o si Upstash falla/timeoutea (1.5s) la request pasa; red final = spending limits. tts.js unifica CORS a ALLOWED_ORIGIN (antes * hardcodeado). IP de x-nf-client-connection-ip (fallback x-forwarded-for) topada a 45 chars. Test test/ratelimit.test.js (12 checks, rojo-primero por módulo ausente + sabotaje manual de count<=limit) enganchado a verify.sh. Reviewer APROBADO CON OBSERVACIONES (2 menores aplicadas) + security-auditor 0 altos (secreto limpio, sin inyección Redis, CORS OK). verify full verde. Env vars ya cargadas por el usuario (ALLOWED_ORIGIN=https://incomparable-puffpuff-76e538.netlify.app + UPSTASH_*). PENDIENTE verificación e2e contra el deploy (loop de curls → 429 + check de spoofeo de x-nf-client-connection-ip). Ítems 3 (spending limits) y 4 (concurrencia) son acción manual, sin código.
